/** Custom notification sound (basename ohne Extension) — gebündelt via expo-notifications `sounds` in app.json. */
export const DRIVER_RIDE_OFFER_PUSH_SOUND = "ride_alert";

/**
 * Android 8+: Kanal-Sound ist nach Erstellung fix — neuer ID-Stand für Custom-Sound.
 * (Alter Kanal `ride-offers` hatte `default`.)
 */
export const DRIVER_RIDE_OFFER_CHANNEL_ID = "ride-offers-v2";
