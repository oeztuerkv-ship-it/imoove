# Umgebungsvariablen & Geheimnisse (nie committen)
.env
.env.*
!.env.example
!.env.deploy.example

# Lokaler Deploy/Import (SSH-Host o. Ä., nicht committen)
.env.deploy

# Firebase Admin / GCP Service Accounts (niemals committen)
**/*firebase-adminsdk*.json
**/*-adminsdk-*.json

node_modules/
.pnpm-store/

# Lokaler Alt-Backup-Ordner (nicht versionieren)
artifacts/mobile/_backup-2026-04-07-stable/

# OS / Editor / Logs (kein Repo-Müll)
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Optional: lokale Tool-Caches (falls im Repo-Root entstehen)
.turbo/
.cache/
coverage/
