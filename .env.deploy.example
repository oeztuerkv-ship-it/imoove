# Kopie nach `.env.deploy` (gitignored). Für lokalen Admin-Panel-Import.
#
# Variante A — direkt per SSH/rsync:
# ADMIN_SERVER=root@DEIN_HOST
#
# Variante B — Ordner liegt schon lokal (z. B. nach manuellem scp):
# ADMIN_LOCAL_PATH=/pfad/zum/admin-panel

ADMIN_SERVER=
# ADMIN_LOCAL_PATH=
